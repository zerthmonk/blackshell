version: '3'

services:
  
  builder:
    image: blackshell
    build:
      context: .
    volumes:
      - ./app:/app
      - node_modules:/app/node_modules
    command: >
      sh -c "npm run dev"
    ports:
      - "0.0.0.0:7777:8080"

volumes:
  node_modules: {}
